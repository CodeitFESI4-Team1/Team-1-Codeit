/* import type { Meta, StoryFn } from '@storybook/react';
import Hearts, { HeartProp } from '.';

const meta: Meta<HeartProp> = {
  title: 'components/Hearts',
  component: Hearts,
};

export default meta;

const Template: StoryFn<HeartProp> = function Template(args: HeartProp) {
  return <Hearts {...args} />;
};

export const OnePoint = Template.bind({});
OnePoint.args = {
  point: 1,
};
*/
export default {};
